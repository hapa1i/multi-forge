# Claude Code guidance

@AGENTS.md

`AGENTS.md` is the authoritative repository instruction file. The import above gives Claude Code the same shared
guidance as other agents. Update shared rules in `AGENTS.md` instead of copying them into this file.

Use `CLAUDE.md` only for Claude Code-specific entry-point behavior. There are currently no Claude-only overrides. Follow
the documentation routing in the imported `AGENTS.md` and load domain documents in proportion to the task.
