# Template: openrouter-glm
# GLM / Z.ai models via OpenRouter (direct, no LiteLLM)
#
# Haiku uses GLM 4.7 Flash. Sonnet/opus use GLM 5.3.
# Requires OPENROUTER_API_KEY in .env (or environment).

proxy:
  family: glm
  preferred_provider: openrouter
  backend: openrouter
  default_port: 8103

  openrouter:
    allow_non_zdr: false
    tiers:
      haiku: z-ai/glm-4.7-flash
      sonnet: z-ai/glm-5.3
      opus: z-ai/glm-5.3
    model_alternatives:
      haiku:
        glm-5.3-flash: z-ai/glm-5.3-flash
        glm-5.3-flashx: z-ai/glm-5.3-flashx
      sonnet:
        glm-5.3-flash: z-ai/glm-5.3-flash
        glm-5.3-flashx: z-ai/glm-5.3-flashx
      opus:
        glm-5.3-flash: z-ai/glm-5.3-flash
        glm-5.3-flashx: z-ai/glm-5.3-flashx
    tier_overrides:
      sonnet:
        reasoning_effort: high
      opus:
        reasoning_effort: max
    error_hints: true
