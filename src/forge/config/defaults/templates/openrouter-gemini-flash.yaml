# Template: openrouter-gemini-flash
# Gemini 3.6 Flash via OpenRouter (cheap, all tiers use Flash)
#
# Requires OPENROUTER_API_KEY in .env (or environment).
#
# All tiers use Flash, differentiated by reasoning_effort.

proxy:
  family: gemini
  preferred_provider: openrouter
  backend: openrouter
  default_port: 8099

  openrouter:
    tiers:
      haiku: google/gemini-3.6-flash
      sonnet: google/gemini-3.6-flash
      opus: google/gemini-3.6-flash
    tier_overrides:
      haiku:
        reasoning_effort: low
      sonnet:
        reasoning_effort: medium
      opus:
        reasoning_effort: high
    error_hints: true
