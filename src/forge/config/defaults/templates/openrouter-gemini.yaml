# Template: openrouter-gemini
# Google Gemini models via OpenRouter (direct, no LiteLLM)
#
# Access to Gemini 3.1 Pro and Gemini 3.8 Flash.
# Requires OPENROUTER_API_KEY in .env (or environment).

proxy:
  family: gemini
  preferred_provider: openrouter
  backend: openrouter
  default_port: 8097

  openrouter:
    allow_non_zdr: false
    tiers:
      haiku: google/gemini-3.8-flash
      sonnet: google/gemini-3.1-pro-preview
      opus: google/gemini-3.1-pro-preview
    tier_overrides:
      sonnet:
        reasoning_effort: medium
      opus:
        reasoning_effort: high
    error_hints: true
