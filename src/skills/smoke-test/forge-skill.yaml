schema_version: 1
name: smoke-test
description: Read-only Forge installation health check. No writes, no test repo needed.
runtimes: [claude_code, codex]
capabilities: [invocation_policy, packaged_script]
allow_implicit_invocation: false
claude_frontmatter:
  allowed-tools: Bash
