# Harden walkthrough sandbox provenance checklist

Current focus: implementation and behavioral verification are complete; finish independent review and merge before
closing this member and Wave 6.

## Phase 1: Activation and evidence

- [x] Close PR #176 bookkeeping on local `main` at `180c2bba`.
- [x] Create `agent/harden-walkthrough-sandbox-provenance`, move this member to `doing/`, and repoint inbound board
  links.
- [x] Recheck O036 against merged production code at `88ac88c5`; both the non-canonical target and pre-provenance
  `env.sh` execution remain live.
- [x] Retain fail-first regressions and a valid generated-repo control before production changes.
- [x] Run the retained slice on unchanged production code (`3 failed, 1 passed` on `88ac88c5`).

## Phase 2: Provenance boundary

- [x] Resolve `FORGE_TEST_REPO` to its canonical real path before denylist comparison.
- [x] Validate the marker and required walkthrough structure before sourcing target-controlled `env.sh`.
- [x] Preserve the default target, six safety checks, command execution, and setup/reset behavior.

## Acceptance tests

| Finding | Fixture                                                 | Assertion                                                     | Test file                                                          |
| ------- | ------------------------------------------------------- | ------------------------------------------------------------- | ------------------------------------------------------------------ |
| O036    | target without marker containing a side-effect env file | wrapper rejects it before the env file executes               | `tests/regression/test_bug_o036_walkthrough_sandbox_provenance.py` |
| O036    | marked target missing required structure                | wrapper rejects it before the env file executes               | `tests/regression/test_bug_o036_walkthrough_sandbox_provenance.py` |
| O036    | symlink alias to the configured home                    | canonical denylist rejects the alias before command execution | `tests/regression/test_bug_o036_walkthrough_sandbox_provenance.py` |
| O036    | repo generated by `setup-test-repo.sh`                  | isolated homes are exported and command runs in the test repo | `tests/regression/test_bug_o036_walkthrough_sandbox_provenance.py` |

## Verification and closeout

- [x] Run the focused walkthrough and retained regression slices (`4` retained and `98` focused tests passed).
- [x] Build the sdist/wheel, compare the bundled wrapper byte-for-byte with source, and run it from a clean temporary
  wheel install.
- [x] Run the targeted packaged-extension Docker integration (`1 passed, 22 deselected`), full regression (`898
  passed`), and unit (`9004 passed, 1 skipped, 122 deselected`) gates.
- [x] Run full pre-commit and final board integrity checks (298 Markdown files, 723 local links, no missing targets, and
  a clean diff check).
- [x] Synchronize the bundled skill safety model; no normative design or end-user update is required because the fix
  restores its documented wrapper boundary without changing CLI or setup behavior.
- [ ] Review and merge independently; then add the change-log entry, move this member and the completed Wave 6 epic to
  `done/`, and update parent/review links.
